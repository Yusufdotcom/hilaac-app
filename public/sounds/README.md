# Order sound files (optional)

Place custom audio files here for production-quality alerts:

- `chime.mp3` — customer status page when order becomes ready
- `ready.mp3` — waiter dashboard when an order becomes ready

If these files are missing, the app falls back to synthesized Web Audio tones automatically.
