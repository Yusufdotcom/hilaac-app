NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Database password for Supabase CLI migrations (db push / db query).
# Required on IPv4-only networks: direct DB host is IPv6-only, so CLI uses
# the pooler + this password (not the flaky cli_login_postgres temp role).
# Dashboard → Project Settings → Database → Database password
SUPABASE_DB_PASSWORD=
NEXT_PUBLIC_APP_NAME="Hilaac"
NEXT_PUBLIC_APP_URL=https://hilaac.so
OPENAI_API_KEY=
ENCRYPTION_SECRET_KEY=
CRON_SECRET=
EVC_API_BASE_URL=
EDAHAB_API_BASE_URL=
CHARGE_TOKEN_SECRET=
EVC_WEBHOOK_SECRET=
EDAHAB_WEBHOOK_SECRET=
